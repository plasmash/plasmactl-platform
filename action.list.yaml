runtime: plugin
action:
  title: List Platforms
  description: "List all platforms in the current repository"
  options:
    - name: format
      shorthand: o
      title: Output Format
      description: Output format (table, json, yaml)
      type: string
      default: "table"
